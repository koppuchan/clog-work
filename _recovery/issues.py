ISSUES = [
# ============ インフラ ============
("[infra] 新VPSの初期構築", """Ubuntu 24.04 上に旧環境と同等のスタックを構築する。

- nginx
- PHP 8.4 + PHP-FPM（ソケット: /run/php/php8.4-fpm.sock）
- MySQL
- Node.js / npm（npm run build 用）
- Composer

アプリ配置は /var/www/attendance-web、ドキュメントルートは /var/www/attendance-web/public。

参考: _recovery/deploy/DEPLOY_RUNBOOK.md, _recovery/deploy/nginx-clog-work.jp.conf"""),

("[infra] ドメイン・DNS・SSL設定", """- clog-work.jp / www.clog-work.jp のAレコードを新VPSへ向ける
- ネームサーバーは ns1-3.xvps.ne.jp。VPSパネルのDNS設定で編集する（レンタルサーバー側のDNS設定は反映されない）
- certbot で Let's Encrypt 証明書を取得し https へリダイレクト
- .env の APP_URL を https://clog-work.jp に設定

注意: 旧VPSが稼働中の間は切り替えないこと。動作確認後に切り替える。"""),

("[infra] メール送信設定とGmail到達性の確保", """会社新規登録時の確認メールが届かない問題の恒久対策。

旧環境の問題点:
- SPF が `v=spf1 ip4:162.43.90.89 ~all`（VPSのIPのみ）
- MXレコードが存在しない
- 逆引きが x162-43-90-89.static.xvps.ne.jp の汎用ホスト名
- .env の MAIL_MAILER が log のままだった可能性

対応:
- 外部SMTP経由での送信に切り替える
- SPF / DKIM / DMARC を設定
- 実際にGmail宛へ到達することをテストで確認（迷惑メールフォルダも含む）

送信箇所: app/Services/RegistrationService.php の Mail::to($email)->send(...)（同期送信）"""),

("[infra] デプロイ手順の整備", """- リポジトリからのデプロイ手順を確立する
- composer install --no-dev --optimize-autoloader
- npm ci && npm run build
- php artisan config:clear 等のキャッシュ処理
- ロールバック手順の用意

参考: _recovery/deploy/deploy.sh, _recovery/deploy/rollback.sh"""),

("[infra] DBの自動バックアップ", """旧環境では毎日深夜3時に取得・30日保持の設定が入っていた。

要確認: 新VPSでビジネスプランの自動バックアップを利用する場合は不要になる可能性がある。櫻本さんの意向を確認したうえで判断する。"""),

# ============ 当初依頼の4点 ============
("[初期4点] 事業所（会社）の削除機能を追加する", """スーパー管理画面から事業所を削除できるようにする。

## 背景
テスト登録した事業所に紐づくメールアドレスが解放されず、再登録できない。
対象: Smile circle株式会社 / izumi@...

## 調査済みの前提
- companies を参照する外部キー16本はすべて ON DELETE CASCADE。会社を消せば関連データは自動削除される
- ただし users は user_companies 経由の多対多。会社を消しても users レコードと email の UNIQUE 制約は残る
- したがってメールアドレスを解放するには、その会社にのみ所属するユーザーの削除も必要

## 受け入れ条件
- [ ] スーパー管理画面の事業所一覧に削除操作がある
- [ ] 削除時に確認ダイアログが出る
- [ ] 削除後、当該メールアドレスで新規登録できる
- [ ] 他の事業所のデータに影響がない"""),

("[初期4点] 新規事業所作成時の管理者個人コードを固定値にする", """## 背景
新規事業所を作成すると管理者の個人コードが全事業所を通した連番で振られるため、その事業所だけコード体系が不揃いになる（例: 会社000002の管理者が000007）。

## 原因（特定済み）
app/Services/RegistrationService.php の generateEmployeeCode() が getMaxEmployeeCode()（全ユーザー横断の最大値）を使用している。

## 実装方針
- employee_code にDBのUNIQUE制約は無くインデックスのみ。会社ごとの一意性はアプリ層で担保しているため、全事業所の管理者に同じ固定値を割り当ててよい
- completeRegistration() 内の採番呼び出し1箇所を固定値に変更する
- 桁数は6桁ゼロ埋めのため 009999

要確認: 固定値を 009999 として良いか櫻本さんに確認する。

## 受け入れ条件
- [ ] 新規事業所作成時、管理者に固定コードが割り当たる
- [ ] 既存ユーザーの個人コードは一切変化しない
- [ ] 一般スタッフの採番ロジックは変更しない"""),

("[初期4点] 会社新規登録時のメールが届かない問題を修正する", """## 背景
新規事業所の登録時に確認メールが届かない。

## 調査済みの前提
- 送信処理は Mail::to($email)->send(new RegistrationVerificationMail(...)) の同期送信。キューワーカー停止が原因の線は消えている
- 原因は .env の MAIL_MAILER 設定か、DNS（SPF / MX / 逆引き）に起因する到達性のいずれか
- いずれの場合もアプリケーションコードの変更は不要

インフラ側の対応は「[infra] メール送信設定とGmail到達性の確保」を参照。

## 受け入れ条件
- [ ] 新規登録時にGmail宛へメールが到達する
- [ ] 送信ログで成否が追跡できる"""),

("[初期4点] 個人マスタにシフト表非表示フラグを追加する", """管理者などをシフト表に表示しないようにする。

## 実装方針
既に is_stamp_hidden（打刻画面を非表示）という同型の実装があるため、同じ形で追加する。

    $table->boolean('is_shift_hidden')->default(false)
        ->comment('シフト表を非表示にする場合TRUE');

default(false) のため既存レコードの挙動は変わらない。

## 実装前に要確認
- 非表示にした人を部門合計・全体合計の人数カウントから除外するか（現状、管理者を含めて「本社合計 4人」と表示されている）
- CSV / Excel 出力に含めるか

## 受け入れ条件
- [ ] スタッフ編集画面でフラグを設定できる
- [ ] シフト管理画面の一覧から除外される
- [ ] 既存スタッフの表示は変化しない"""),

# ============ 6/3以降の再実装 ============
("[再実装] スーパー管理画面（/super-admin）を実装する", """6/3以降に追加された画面。SaaS運営者用の管理画面。

旧本番のビルド成果物から以下4画面の存在を確認済み。

- SuperAdmin/Dashboard.tsx
- SuperAdmin/Companies.tsx
- SuperAdmin/NewCompany.tsx
- SuperAdmin/Users.tsx

ルート: /super-admin, /super-admin/companies, /super-admin/companies/new, /super-admin/users

事業所の削除機能（当初依頼の1点目）はこの画面に載せる。"""),

("[再実装] FeliCa打刻APIエンドポイントを実装する", """既存のFeliCa打刻アプリ v0.2.6 をそのまま利用できるようにする。

## 必要なAPI（復元済みの仕様）

    POST /stamp/{companyUuid}/felica
    Content-Type: application/json

    Body: { idm: 16桁小文字16進, intent: break-start（休憩開始モード時のみ、通常は省略） }

    成功条件: HTTP 2xx かつ レスポンス body の success === true

6/3時点のソースには /stamp/{uuid}/clock-in 等は存在するが、felica のみ不足している。

## DBカラム追加

    ALTER TABLE users
      ADD COLUMN felica_idm VARCHAR(16) DEFAULT NULL,
      ADD COLUMN felica_registered_at TIMESTAMP NULL DEFAULT NULL;
    ALTER TABLE users ADD UNIQUE KEY uk_users_felica_idm (felica_idm);

詳細は _recovery/README.md および _recovery/felica-app-src/src/stamp-api.js を参照。"""),

("[再実装] スタッフ編集画面にFeliCaカード登録UIを追加する", """felica_idm をスタッフに紐づけるための管理画面UI。カードをかざして登録する導線を用意する。"""),

("[再実装] Excel出力：テンプレートの計算式を維持する", """PhpSpreadsheet で全セルに値を書き込むとテンプレートの数式が消えるため、データ範囲のみ書き込み数式セルには触れない方式にする（チャットで案Aを採用済み）。

実物の出力ファイルから復元した仕様は _recovery/README.md を参照。サマリ欄には備考・申請列を解析する SUMPRODUCT 式が19本入っている。"""),

("[再実装] Excel/CSV出力：10名単位のZip分割と一括ダウンロード", """人数が多い場合のサーバー負荷対策。チャットでは案A+案Bの併用で合意。

- 案A: 10名ごとのバッチ指定ダウンロード
- 案B: 一括DLボタン → 内部で10名ずつ生成 → Zipにまとめて返却"""),

("[再実装] 休憩2枠対応", """帳票および画面で休憩を2枠まで扱えるようにする（休憩入1 / 休憩出1 / 休憩入2 / 休憩出2）。"""),

("[再実装] 一覧のヘッダー固定表示", """スクロール時にヘッダー行を固定する。"""),

("[再実装] 日跨ぎ勤務の遅刻・早退・残業計算ロジック", """日付をまたぐ勤務における遅刻・早退・残業の判定を修正する。"""),

("[再実装] 夜勤の24時間ルールと打刻漏れ判定", """日跨ぎ夜勤の出退勤紐付け計算、打刻漏れ時の制御・判定ロジック。"""),

("[再実装] 重複打刻の防止ロック", """短時間に連続でカードをかざした場合の多重打刻を防ぐ。"""),

("[再実装] 休憩開始モード（intent=break-start）", """打刻画面に休憩開始モードを設ける。FeliCaアプリ側は intent: break-start を送信する実装になっている。"""),

("[再実装] 従業員側画面への労務アラート表示", """スタッフ画面に労務アラートを表示する。"""),

("[再実装] 月間サマリの表示項目を整理する", """月間サマリの項目構成を見直す。"""),

("[再実装] 打刻修正・申請表示の挙動改善", """打刻修正および申請の表示まわりの改善。"""),

("[再実装] 打刻レスポンスの高速化", """打刻処理のレスポンス改善。"""),

("[再実装] 管理者用のスタッフ検索（名前・個人コード）", """管理画面のスタッフ一覧に検索機能を追加する。"""),

("[再実装] データ表示期間を過去66ヶ月に拡張する", """過去5.5年分のデータを参照できるようにする。"""),

("[再実装] 生打刻時刻と丸め時刻の役割を分離する", """画面表示は生の打刻時刻、内部計算は丸め時刻（15分単位等）という役割分担を明確にする。"""),

("[再実装] 休暇申請の承認時に自動減算・表示する（B機能）", """シフト管理画面において、休暇申請が承認された際に残日数を自動で減算し表示に反映する。

旧契約では追加機能として4万円で発注された項目。"""),

("[再実装] シフト設定に合わせた未打刻時休憩の自動入力（トグル付き）", """打刻が無い場合にシフト設定の休憩を自動補完する。トグルでON / OFF切り替え。

付随する表示仕様:
- 自動入力された休憩はグレー表示
- 15分丸め処理
- 片方だけ打刻がある場合の表示
- 紫色の「休憩漏れ」バッジ

旧契約では追加機能として5万円で発注された項目。"""),

("[再実装] アラートバッジと警告トースト（退勤忘れ・未計算）", """「退勤忘れ」「未計算」等の状態をバッジとトーストで通知する。"""),

("[再実装] 打刻修正バッジの視認性改善", """オレンジ色の時刻をクリックすると修正履歴を参照できるようにする。"""),

("[再実装] スタッフ呼称の統一（ユーザー→スタッフ）", """画面上の表記を「スタッフ」に統一する。"""),

("[再実装] スタッフ画面シフトテーブルへの休暇バッジと部門集計", """スタッフ側のシフト表に休暇バッジを表示し、部門ごとの集計を追加する。"""),

("[再実装] 夜勤登録時の休憩時間範囲の検証", """範囲外の休憩時間が登録された場合のエラー処理。"""),

("[再実装] 打刻トーストの文言調整", """例:「退勤（日付越え）を記録しました」。"""),

("[再実装] CSV/Excel出力の最適化（片方打刻休憩・休日区分）", """片方だけ打刻された休憩の扱い、休日区分の出力形式を調整する。"""),

("[再実装] LPサブドメイン（lp.clog-work.jp）のDNS確認", """ランディングページ用サブドメイン。

現在はレンタルサーバー（sv311 / 202.226.39.151）側でWordPressが稼働しており移行対象外。新VPSへの切り替え時にDNSが影響を受けないよう注意する。"""),

# ============ 移行 ============
("[移行] FeliCa打刻端末の設定変更とカード再登録", """新サーバーでは会社のUUIDが変わるため、端末ごとにアプリの設定を更新する必要がある。

- アプリの設定画面で serverUrl と companyUuid を入れ直す（再インストールは不要）
- felica_idm は旧DBから取り出せないため、全カードの再登録が必要

設定は electron-store（felica-stamp-config）に保存される。"""),

("[移行] 旧環境からのデータ移行", """旧VPSにはSSHでアクセスできないため、DBダンプは取得できない。管理画面からCSV / Excelで出力できる範囲のみ移行を試みる。

- シフト
- 勤務実績
- スタッフ一覧

移行できないもの: パスワード（ハッシュ）、FeliCaカード登録、打刻履歴の全量。

旧VPSは移行完了まで削除しないこと。"""),

("[移行] 旧環境の設定値を新環境に再現する", """管理画面の設定画面から値を控えて再設定する。

- 労務アラートの閾値
- シフトパターン
- 部門
- 打刻の丸め単位
- 給与締切日（20日であることは出力帳票の期間から確定済み）

旧VPSを停止する前にスクリーンショットで記録しておく。"""),
]
